
Print "this is just exploring some syntax"

-- comments like this

-- expressive statements (=<-)

-- by <- you are saying that the following statement should and must return a value
a =<- If (c > x) {10+20} Else 10

-- If not condition then a is void


age = 20
ageLimit = 30

result =<- By age, name, & ageLimit (a n l,
    "{n} is old enough"   If (a>l)
    "{n} is old enough"   If (a==l)
    "Not old enough"
)










